package com.example.android.snoopyea.models;

public class EventsAdapter /*extends RecyclerView.Adapter<EventsAdapter.EventHolder> {

    private LayoutInflater inflater;
    private List<EventItem> listData;
    private ItemClickCallback itemClickCallback;

    public interface  ItemClickCallback{
        void onItemClick(int p);
    }

    public EventsAdapter(List<EventItem> list, Context context) {

        this.inflater = LayoutInflater.from(context);
        this.listData = list;
    }

/*
    @Override
    public EventHolder onCreateViewHolder(ViewGroup parent, int viewType) {

            View view = inflater.inflate(R.layout.eventItem, parent, false);
            return view;

    }

    @Override
    public void onBindViewHolder(EventHolder holder, int position) {

    }

    @Override
    public void onBindViewHolder(RecyclerView.ViewHolder holder, int position) {

        EventItem item = listData.get(position);
        holder.
    }

    @Override
    public int getItemCount() {
        return listData.size();
    }


    class EventHolder extends RecyclerView.ViewHolder implements View.OnClickListener {

        private TextView text;
        private TextView subText;

        public EventHolder(View itemView) {
            super(itemView);

            text = (TextView) itemView.findViewById(R.id.lbl_text);
            subText = (TextView) itemView.findViewById(R.id.lbl_subtext);

            itemView.setOnClickListener(this);

        }

        @Override
        public void onClick(View v) {

        }
    }*/
        {
}